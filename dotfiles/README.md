# Dotfiles

Use this directory as a placeholder for configuration files or exported
settings that are worth restoring across machines.

Keep this directory generic in the scaffolding phase. Add app-specific
subdirectories or files only when you are ready to track real configuration.
