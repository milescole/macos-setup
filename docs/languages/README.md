# Languages

Use this section for runtime, package-manager, and toolchain documentation.

Each category gets its own folder with a `README.md` landing page, and each
individual language or tool should get its own file inside that folder.

Examples of the intended shape:

- `languages/runtimes/<language>.md`
- `languages/package-managers/<tool>.md`
- `languages/toolchains/<tool>.md`
