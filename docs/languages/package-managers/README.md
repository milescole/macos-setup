# Package Managers

Use this folder for runtime-specific and language-specific package managers.

Add one file per package manager and keep this page focused on shared rules
about global installs, project-local dependencies, and update policy.
