# Machine Rebuild

For a fresh machine rebuild:

1. Install baseline developer prerequisites.
2. Clone this repository.
3. Run `./bootstrap/bootstrap.sh`.
4. Review any manual application sign-ins, config restores, or post-install steps.
5. Run `./scripts/doctor.sh` to verify the setup.

Keep this page focused on the shortest reliable path from a blank machine to a
working development environment.
