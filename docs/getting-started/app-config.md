# Configuration

Use this page to document which settings are worth exporting and restoring
across machines.

In the scaffolding phase, keep this intentionally generic:

- which tools or apps should eventually have tracked configuration
- where their exported settings live in the repository
- what restoration steps should be automated versus documented
