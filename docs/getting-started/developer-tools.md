# Developer Tools

Use this page to document the baseline developer tooling that should exist
before the rest of the machine setup is applied.

In the scaffolding phase, keep this generic:

- which developer tool prerequisites should be installed first
- which prerequisites are manual versus automated
- which later setup steps depend on these prerequisites
